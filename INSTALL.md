# 📖 身后名 · 安装教程（小白版）

> 不会代码也能装。跟着下面 4 步走，全程 3 分钟。

---

## 第 1 步：下载插件文件

1. 浏览器打开：`https://github.com/Hejiankun7/shenhouming`
2. 页面是英文的没关系，找绿色的 **「Code」** 按钮（页面右侧）
3. 点它 → 下拉菜单里点 **「Download ZIP」**
4. 浏览器会下载一个 `shenhouming-main.zip` 压缩包

![提示] 如果下载很慢，可以用国内的加速镜像站，例如 `https://ghproxy.com/https://github.com/Hejiankun7/shenhouming/archive/refs/heads/main.zip`

## 第 2 步：解压

1. 双击压缩包，系统会自动解压出一个文件夹（叫 `shenhouming-main`）
2. **把这个文件夹放到桌面或下载文件夹都行**，记住它的位置
3. ⚠️ 关键：文件夹**不能删、不能改名**，插件运行时需要它

## 第 3 步：装进浏览器

### Microsoft Edge（推荐，Windows 自带）

1. 打开 Edge 浏览器，地址栏输入：`edge://extensions` 回车
2. 页面**左下角**找到 **「开发人员模式」**，把开关**打开**（变蓝）
3. 页面**左上角**出现 **「加载解压缩的扩展」** 按钮，点它
4. 在弹窗里**选中第 2 步那个文件夹**（`shenhouming-main`），点确定
5. 浏览器顶部工具栏出现 🕊️ 图标 = 安装成功！

### Google Chrome（谷歌浏览器）

1. 打开 Chrome，地址栏输入：`chrome://extensions` 回车
2. 右上角打开 **「开发者模式」** 开关
3. 点 **「加载已解压的扩展程序」**，选中文件夹
4. 工具栏出现 🕊️ 图标 = 成功

### 国内浏览器（360 / QQ / 搜狗 / 猎豹）

1. 确认浏览器处于**极速模式**（不是兼容模式）
2. 找到"扩展程序"页面（一般在"更多工具"或设置里，或地址栏输入 `se://extensions` / `qqbrowser://extensions`）
3. 打开"开发者模式"→"加载已解压的扩展程序"→ 选中文件夹

### 不支持的浏览器

- ❌ **Safari**（苹果自带）——内核不同，装不了
- ❌ **Firefox**（火狐）——不支持 Manifest V3，装不了

## 第 4 步：开始使用

1. 点工具栏的 🕊️ 图标，打开控制面板
2. **别动任何设置**，先点底部的 **「⚰️ 假装我已死」** 按钮测试
3. 如果显示「本可清除 N 条历史，将写入 XX 条（未实际执行）」= 一切正常
   （这是安全的干跑测试，**不会删任何东西**）
4. 确认没问题后，想启用真实功能：
   **取消勾选「干跑模式」→ 点「💾 保存设置」**
5. 之后连续 N 天（默认 14 天）不用浏览器，就会自动清空历史 + 写入伪造的积极记录

---

## ⚠️ 必读提醒

1. **千万别一上来就关掉「干跑模式」**——那会真的清空全部浏览历史！先干跑测试看清楚再决定
2. 插件面板里的「国内网络适配」建议保持开启——只写国内能访问的网站，更真实
3. 想卸载：回到扩展管理页，点插件卡片上的删除/移除按钮即可，**你的浏览器历史不受任何影响**
4. 更新版本：重新下载 zip → 解压覆盖旧文件夹 → 扩展页点 🕊️ 卡片上的 🔄 刷新

## ❓ 常见问题

| 问题 | 解决 |
|---|---|
| 面板打开显示"加载中..."不动 | 扩展页点 🕊️ 卡片上的 🔄 刷新图标 |
| 提示"读取和更改浏览记录" | 点允许——这是清空/写入历史的必要权限 |
| 点了「假装我已死」没反应 | 先点「💾 保存设置」，再点测试按钮 |
| 装完没看到 🕊️ 图标 | 可能被藏在"拼图"图标里，点浏览器右上角的拼图展开 |
| 找不到"开发人员模式" | 确认你打开的是 `edge://extensions` 或 `chrome://extensions` 这个页面 |

---

*有问题欢迎在 GitHub 提 Issue，或直接联系作者。*
